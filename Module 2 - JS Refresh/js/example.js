// Without Export Default, you can export as many variables as you want.
const firstLine = 'Hello World!';

// This will import the value and not a variable.
// Can only have ONE export default per module file
export default 'Revisiting JS';

const weather = "It's snowing";

export { firstLine, weather };
